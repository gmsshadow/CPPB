"""PyQt6-based desktop editor for Cortex character files.

Importing anything from this subpackage will pull in PyQt6, so the CLI
deliberately does not import editor.* anywhere. PyQt6 is an optional
dependency declared under [project.optional-dependencies].editor in
pyproject.toml.
"""
